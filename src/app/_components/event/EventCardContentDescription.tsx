const EventCardContentDescription = ({ description }: { description: string }) => {
	return (
		<p className="pointer-events-none mt-2 block font-medium text-gray-500">{description}</p>
	)
}

export default EventCardContentDescription 
